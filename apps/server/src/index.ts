/*
 * Trackit server — scaffold only.
 *
 * Nothing runs here yet. The package exists so the workspace already has the
 * shape it will grow into (Express + PostgreSQL), and so packages/shared has a
 * second consumer from day one — which keeps it honest about never assuming a
 * DOM, an Electron bridge, or anything else only the desktop app has.
 */
export {}
