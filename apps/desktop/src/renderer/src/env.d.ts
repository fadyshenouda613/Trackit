/* Vite's ambient types for the renderer — `import.meta.env` (DEV, PROD, MODE)
   and the asset module declarations. A reference rather than a `types` array in
   tsconfig.web.json, which would switch off automatic @types inclusion. */
/// <reference types="vite/client" />
